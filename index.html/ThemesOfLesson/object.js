'use Strict'

let user = {};
user['name'] = 'Вася';
let birth = 2005;
const user{
    name : name,
    birth : birth = 5,
};
console.log(user.birth1);

function User(name, birth){
    title.name = name;
    title.birth = birth;
    title.sayHello = function(){
        console.log('привет! Меня зовут ' + title.name)
    }
}
let user1 = new User('Вася', '2007');
